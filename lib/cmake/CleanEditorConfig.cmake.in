get_filename_component(CleanEditor_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
include(CMakeFindDependencyMacro)

list(APPEND CMAKE_MODULE_PATH ${CleanEditor_CMAKE_DIR})

find_dependency(Qt5 REQUIRED COMPONENTS Core)

list(REMOVE_AT CMAKE_MODULE_PATH -1)

if(NOT TARGET CleanEditor::CleanEditor)
    include("${CleanEditor_CMAKE_DIR}/CleanEditorTargets.cmake")
endif()

set(CleanEditor_LIBRARIES CleanEditor::CleanEditor)
